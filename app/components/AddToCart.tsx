'use client';
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("Hello Log")}>Add To Cart</button>
    </div>
  );
};

export default AddToCart;
